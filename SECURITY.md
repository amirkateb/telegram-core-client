# Security Policy

Do not open public issues containing credentials, consumer secrets, Telegram bot tokens, signed payloads, private URLs, or production logs. Rotate a credential immediately if it may have been exposed. The SDK never requires a Telegram Bot Token; Bot Tokens belong to Telegram Core.
