# Contributing

Use PHP 8.1+, keep public APIs backward compatible within a major version, never add direct `api.telegram.org` calls, add tests for signature/transport changes, and update README/docs/CHANGELOG together with contract changes. Never commit credentials or generated vendor files.
